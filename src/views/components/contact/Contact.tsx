import React from "react";
import { Box } from "@mui/material";
// import { useTranslation } from "react-i18next";

const Contact = () => {
  // const { t } = useTranslation("landingView");

  return (
    <Box height="500px" sx={{ background: 'white' }}>
     
    </Box>
  );
};

export default Contact;
